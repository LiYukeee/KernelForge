# define optimize target
TARGET="../solution/sample_relu"

# define LLMs
MODEL_NAME=
BASE_URL=
API_KEY=
MODEL_PROVIDER=deepseek
# Per-request resilience. The API gateway may perform additional retries itself.
MODEL_MAX_RETRIES=2
MODEL_TIMEOUT_SECONDS=180

# Retry the read-only PLAN stage after transient API failures.
PLAN_MAX_ATTEMPTS=3
PLAN_RETRY_BASE_DELAY_SECONDS=5
PLAN_RETRY_MAX_DELAY_SECONDS=60
# define MACA architecture list and paths (optional)
export MACA_HOME=/opt/maca-3.3.0
PYTHON_BIN=/home/yuke/miniforge-pypy3/envs/torch_new/bin/python

# define CUDA architecture list and paths (optional)
PYTHON_BIN=/home/liyk/miniconda3/envs/llm/bin/python
TORCH_CUDA_ARCH_LIST="12.0"
CUDA_HOME=/usr/local/cuda-13.2
PATH="$CUDA_HOME/bin:$PATH"
LD_LIBRARY_PATH="$CUDA_HOME/lib64:${LD_LIBRARY_PATH:-}"

# LangSimth
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=kernel-forge-dev
LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# Developer
PRINT_ALL=true
